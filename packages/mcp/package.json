{
  "name": "@continuum/mcp",
  "version": "0.1.0",
  "description": "MCP server for Continuum — exposes context tools to AI agents",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@continuum/core": "workspace:*"
  }
}
